apiVersion: v1
kind: Service
metadata:
  name: {{ include "openfeign.fullname" . }}
  labels:
    {{- include "openfeign.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openfeign.selectorLabels" . | nindent 4 }}
