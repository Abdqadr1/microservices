apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openfeign.fullname" . }}-test-connection"
  labels:
    {{- include "openfeign.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openfeign.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
