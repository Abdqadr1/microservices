apiVersion: v1
kind: Service
metadata:
  name: {{ include "qadr-common.fullname" . }}
  labels:
    {{- include "qadr-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qadr-common.selectorLabels" . | nindent 4 }}
