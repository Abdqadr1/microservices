apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qadr-common.fullname" . }}-test-connection"
  labels:
    {{- include "qadr-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qadr-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
